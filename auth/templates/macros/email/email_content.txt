{%- macro greeting(user) -%}

Hi {{ user | display_name }},

{%- endmacro -%}


{%- macro show_expires_in(expires_in) -%}

This link will expire in {%- if expires_in %} {{ (expires_in / 60) | int }} minutes. {%- else %} 24 hours. {%- endif -%}

{%- endmacro -%}